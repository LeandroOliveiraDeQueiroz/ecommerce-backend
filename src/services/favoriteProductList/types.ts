export interface FavoriteProductListCreateService {
  user_id: number;
  title: string;
  description: string;
}

export interface FavoriteProductListUpdateService {
  user_id: number;
  title: string;
  description: string;
}
